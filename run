#!/usr/bin/bash

cmake -B build -G Ninja || exit
cmake --build build || exit

build/src/main > out.txt || exit

cd visu

if [ ! -d ./venv ]; then
     python3 -m venv venv
fi

venv/bin/manim -qm -p anim.py || exit